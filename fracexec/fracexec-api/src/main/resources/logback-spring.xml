<?xml version="1.0" encoding="UTF-8"?>
<configuration>

  <!-- Profile local/test: log legível no console -->
  <springProfile name="local,test">
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
      <encoder>
        <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
      </encoder>
    </appender>
    <root level="INFO">
      <appender-ref ref="CONSOLE"/>
    </root>
    <logger name="com.fracexec" level="DEBUG"/>
  </springProfile>

  <!-- Profile prod: log JSON estruturado sem PII -->
  <springProfile name="prod">
    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
      <encoder class="net.logstash.logback.encoder.LogstashEncoder">
        <fieldNames>
          <timestamp>timestamp</timestamp>
          <version>[ignore]</version>
        </fieldNames>
        <!-- Nunca incluir stackTrace completo com dados de request que possam conter PII -->
        <throwableConverter class="net.logstash.logback.stacktrace.ShortenedThrowableConverter">
          <maxDepthPerThrowable>10</maxDepthPerThrowable>
          <maxLength>2048</maxLength>
        </throwableConverter>
      </encoder>
    </appender>
    <root level="WARN">
      <appender-ref ref="JSON"/>
    </root>
    <logger name="com.fracexec" level="INFO"/>
    <!-- Silenciar logs do Hibernate SQL em prod -->
    <logger name="org.hibernate.SQL" level="WARN"/>
    <logger name="org.hibernate.type" level="WARN"/>
  </springProfile>

</configuration>
